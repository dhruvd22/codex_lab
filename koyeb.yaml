name: coding-conductor
services:
  - name: conductor
    type: web
    source:
      type: docker
      docker:
        file: Dockerfile
    ports:
      - port: 8000
        http:
          public: true
