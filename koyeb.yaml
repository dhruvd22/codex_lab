name: projectplanner
services:
  - name: planner
    type: web
    source:
      type: docker
      docker:
        file: Dockerfile
    ports:
      - port: 8000
        http:
          public: true
