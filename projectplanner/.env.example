# Base configuration for running projectplanner locally.
# Required: OpenAI key powers agent orchestration and embeddings.
OPENAI_API_KEY=sk-...
# Optional: point to Postgres instead of the default SQLite file DB.
DATABASE_URL=
# Optional: enable pgvector if your Postgres instance has the extension installed.
USE_PGVECTOR=false
# UI will call the FastAPI backend at this base URL.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Optional: send traces to Langfuse if you configure it.
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
